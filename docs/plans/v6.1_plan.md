你是一位專業量化交易系統工程師，請基於目前的 Wick Reversal v6 策略，實作一個改良版本：Wick Reversal v6.1。

目標：
不是單純調參，而是修正 v6 中「策略描述與實作不一致」、「風險不夠 volatility-normalized」、「order-flow/trailing 統計不完整」等問題，讓策略更適合做跨年份、跨價格 regime 的研究。

請保留 v6 的核心邏輯：
1. k0 wick reversal detection
2. ATR / dynamic N
3. wick-zone tick delta 判斷 Absorb / Initiative
4. k0 後 zoom window entry
5. tick-first backtest
6. long / short mirror 基礎架構
7. StrategySignal 輸出格式盡量相容原本系統

請實作以下 v6.1 修改：

============================================================
一、策略名稱與類別
============================================================

新增 class：

WickReversalV6_1Strategy(WickReversalV6Strategy)

name = "Wick Reversal 15m v6.1"

如果原本已有 WickReversalV6_1mStrategy，請不要混淆：
- v6.1 是策略版本
- 1m 是 timeframe variant

必要時可命名為：

WickReversalV61Strategy

並設定：

name = "Wick Reversal 15m v6.1"

============================================================
二、Entry Zone 改成 body reclaim + ATR cap
============================================================

目前 long entry 上限：

max_entry = k0.high + k0_rng * entry_extension_a

請改成：

body_high = max(k0.open, k0.close)
entry_cap = min(k0_rng * entry_extension_a, atr * entry_atr_cap)
max_entry = body_high + entry_cap

long trigger：
price > body_high AND price <= max_entry

short 對稱：

body_low = min(k0.open, k0.close)
entry_cap = min(k0_rng * entry_extension_a, atr * entry_atr_cap)
min_entry = body_low - entry_cap

short trigger：
price < body_low AND price >= min_entry

新增參數：

entry_atr_cap: float = 0.35

注意：
_try_entry_long / _try_entry_short 目前沒有傳入 atr，請修改函式 signature，讓 entry function 可以拿到 atr。

============================================================
三、Stop 改成 range + ATR hybrid stop
============================================================

目前 long stop：

stop_p = k0.low - k0_rng * stop_extension_b

請改成 hybrid stop：

range_stop = k0.low - k0_rng * stop_extension_b
atr_stop   = k0.low - atr * stop_atr_mult
stop_p     = min(range_stop, atr_stop)

short：

range_stop = k0.high + k0_rng * stop_extension_b
atr_stop   = k0.high + atr * stop_atr_mult
stop_p     = max(range_stop, atr_stop)

新增參數：

stop_atr_mult: float = 0.25

注意：
這會讓 stop 至少考慮目前 volatility，避免不同價格 regime 下 risk 結構失真。

============================================================
四、修正 trade-level cum_delta：真正從 entry 後跨 bar 累積
============================================================

目前 _tick_exit_long / _tick_exit_short 裡面每根 bar 重新計算：

cum_buy_vol = 0.0
cum_vol = 0.0
cum_delta = 0.0

這不是 trade-level cum_delta。

請改成使用 class state：

self._tcv   # trade cumulative volume
self._tcbv  # trade cumulative buy volume
self._tcd   # trade cumulative delta

每一個 tick：

self._tcv += qty
if not is_bm:
    self._tcbv += qty

self._tcd = 2.0 * self._tcbv - self._tcv

long：
- 正 delta 代表買方主導
- 負 delta 代表賣方主導

short：
- 負 delta 代表賣方主導
- 正 delta 代表買方主導

請確保 entry 後初始化：

self._tcv = 0.0
self._tcbv = 0.0
self._tcd = 0.0
self._peak_trade_delta = 0.0

============================================================
五、啟用 trade_delta_drawdown_pct
============================================================

目前 trade_delta_drawdown_pct 參數存在，但沒有真正發揮作用。

請實作：

long trailing 中：
- 每根 tick 更新 self._peak_trade_delta = max(self._peak_trade_delta, self._tcd)
- 若 self._peak_trade_delta > 0 且 self._tcd < self._peak_trade_delta * (1 - trade_delta_drawdown_pct)，則 exit，label = "TDD"

short trailing 中：
- 使用 negative delta 越低代表賣方越強
- 可用 self._peak_trade_delta 表示最有利的 negative delta，例如初始化為 0，然後：
  self._peak_trade_delta = min(self._peak_trade_delta, self._tcd)
- 若 self._peak_trade_delta < 0 且 self._tcd > self._peak_trade_delta * (1 - trade_delta_drawdown_pct)，則 exit，label = "TDD"

請注意 short 的數學方向：
假設 peak = -1000，drawdown_pct = 0.3
允許回升到 -700
如果 self._tcd > -700，表示賣方 delta 回吐過多，應該 exit。

============================================================
六、Trailing stop 行為明確化：新增模式參數
============================================================

目前註解和實作不一致。

請新增參數：

trailing_stop_mode: str = "lock_tp"

支援兩種模式：

1. "lock_tp"
   TP touch 且 momentum favor 時：
   long: self._stop_price = target_p
   short: self._stop_price = target_p

2. "breakeven_cost"
   TP touch 且 momentum favor 時：
   long: self._stop_price = self._entry_price + self._rt_cost(self._entry_price)
   short: self._stop_price = self._entry_price - self._rt_cost(self._entry_price)

請實作 helper：

def _activate_trailing(self, side: str, target_p: float) -> None:
    self._trailing = True
    self._td_consec = 0
    if self.trailing_stop_mode == "breakeven_cost":
        if side == "long":
            self._stop_price = self._entry_price + self._rt_cost(self._entry_price)
        else:
            self._stop_price = self._entry_price - self._rt_cost(self._entry_price)
    else:
        self._stop_price = target_p

在 TP touch 且 momentum favorable 時呼叫此 helper。

============================================================
七、保留 TP touch momentum 邏輯，但改用 trade-level delta
============================================================

long：
當 price >= target_p：
- if self._tcd > 0:
    activate trailing
- else:
    direct TP exit

short：
當 price <= target_p：
- if self._tcd < 0:
    activate trailing
- else:
    direct TP exit

============================================================
八、bar fallback 也要盡量一致
============================================================

如果 tick 不存在且 allow_bar_fallback_in_tick_mode = True：
- 可以繼續使用 bar_delta 近似
- 但也應該累加到 self._tcv / self._tcbv / self._tcd
- 不要每根 bar 完全重置 trade delta

例如：

bar_vol = k.volume
bar_buy = k.taker_buy_volume
self._tcv += bar_vol
self._tcbv += bar_buy
self._tcd = 2.0 * self._tcbv - self._tcv

============================================================
九、加入 debug / metadata 訊號欄位，如果 StrategySignal 支援
============================================================

如果 StrategySignal 支援 label 或 extras / metadata，請盡量在 label 中保留資訊：

k0 label：
k0_Absorb
k0_Initiative
k0s_Absorb
k0s_Initiative

entry label：
L6.1
S6.1

exit label：
TP
SL
TS
TD
TDD

如果 StrategySignal 支援附加欄位，請記錄：
- wick_type
- zoom_delta_eff
- k0_range_atr_ratio
- entry_delay_bars
- trailing_stop_mode
- final_trade_delta

如果不支援，不要破壞既有 dataclass。

============================================================
十、避免 lookahead bias
============================================================

請確認：
- ATR / SMA_ATR 只使用當前與過去資料
- dynamic N 只看 k0 之前的 bars
- entry 只在 k0 之後的 bars/ticks 判斷
- exit 不使用未來 bar 資料

============================================================
十一、完成後請輸出
============================================================

請輸出：
1. 修改後完整 Python code
2. 簡短列出 v6 -> v6.1 的修改摘要
3. 標註哪些地方是策略邏輯修正，哪些地方是新增參數
4. 如果有任何相容性風險，請列出

實作風格要求：
- 不要大幅重構整個策略框架
- 儘量延續原本 v6 code style
- 不要引入外部 library
- 保持 tick-first 邏輯
- long / short 都要完整支援
- 不要刪除 v6 原始 class，新增 v6.1 class 或清楚區分版本