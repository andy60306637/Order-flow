你是一位專業量化交易研究員與 Python 交易系統工程師。

目前我有一個策略：Wick Reversal 15m v6.1。
請你基於目前專案中的 v6.1 策略程式與回測輸出，進行「策略診斷、修正、優化實驗與找答案」。

請注意：
這不是要你盲目調參，也不是單純提高報酬率。
我要找的是：
1. v6.1 是否真的比 v6 更合理？
2. 目前虧損主要來自哪個子策略、哪個 regime、哪個 side、哪種 exit？
3. 哪些交易條件有保留價值？
4. 哪些條件應該被移除、加嚴或拆分？
5. 如何把 v6.1 改成更穩定、可跨年份驗證的版本？

============================================================
一、請先檢查 v6.1 程式正確性
============================================================

請先檢查目前 Wick Reversal v6.1 code，不要急著最佳化。

重點檢查：

1. StrategySignal 是否支援 meta 欄位
   - 如果支援，保留 meta。
   - 如果不支援，請提供相容寫法，例如 _make_signal() helper，自動移除 meta。

2. _update_excursion() 是否存在
   - 如果不存在，請補上。
   - 目標是記錄 MAE / MFE。
   - long:
     MFE = max(MFE, high - entry_price)
     MAE = max(MAE, entry_price - low)
   - short:
     MFE = max(MFE, entry_price - low)
     MAE = max(MAE, high - entry_price)
   - 若 entry_risk > 0，請同時輸出 mae_r / mfe_r。

3. _exit_meta() 是否存在
   - 如果不存在，請補上。
   - 至少回傳：
     final_trade_delta
     mae
     mfe
     mae_r
     mfe_r
     trailing_stop_mode
     entry_risk

4. 檢查 TDD 是否只在 trailing 後觸發
   - TDD 不應該在尚未 TP touch / 尚未 trailing 時觸發。
   - 如果目前已修正，請確認邏輯仍正確。

5. 進入 trailing 時，請重設 peak_trade_delta
   - long:
     self._peak_trade_delta = max(0.0, self._tcd)
   - short:
     self._peak_trade_delta = min(0.0, self._tcd)

6. 檢查 stop / entry cap 使用的是 k0 ATR 還是 entry bar ATR
   - 請改成使用 k0 形成當下的 ATR。
   - 在偵測 k0 時儲存 long_k0_atr / short_k0_atr。
   - entry 時傳入 k0_atr，而不是當前 bar 的 atr。

7. 移除未使用且可能造成 import error 的 import
   - 例如 _percentile_rank, _BODY_FLOOR_PCT。
   - 若沒有用到，請刪除。

8. 檢查 bar fallback mode
   - 若 tick mode 缺 tick 且 allow_bar_fallback_in_tick_mode=False，應避免偷用 bar。
   - 若 fallback=True，請讓 bar delta 也能累積到 trade-level delta。

============================================================
二、請修正 summary / export 統計
============================================================

目前交易明細中有 TDD exit，但 summary 只統計 SL / TP / TS / TD，導致 exit count 不完整。

請修正績效報表，至少加入：

1. exit label count:
   - SL
   - TP
   - TS
   - TD
   - TDD

2. 各 exit label 的：
   - trade count
   - win rate
   - net pnl
   - gross pnl
   - fee
   - avg pnl
   - avg R
   - avg mae_r
   - avg mfe_r

3. side-level summary:
   - long
   - short

4. side + regime summary:
   - long + neutral
   - long + range
   - long + trend_up
   - long + trend_down
   - short + neutral
   - short + range
   - short + trend_up
   - short + trend_down

5. wick_type summary:
   - Absorb
   - Initiative

6. side + wick_type summary

7. session hour summary:
   - UTC hour 0~23
   - trade count
   - win rate
   - net pnl
   - PF
   - avg R

8. entry_delay_bars summary:
   - k0 後第幾根 bar 進場
   - trade count
   - win rate
   - net pnl
   - PF
   - avg R

============================================================
三、請基於目前回測結果回答以下問題
============================================================

目前已知 2023-04-14 ~ 2024-04-13 的 v6.1 結果大致是：

- total trades: 約 85
- return: 約 -16.77%
- win rate: 約 30.6%
- profit factor: 約 0.88
- net pnl: 約 -1677 USDT
- fees: 約 2344 USDT
- long trades: 約 55，PF 約 1.04，net pnl 約 +377
- short trades: 約 30，PF 約 0.61，net pnl 約 -2054
- neutral regime 裡 long 明顯較好，short neutral 很差
- exit label 中 SL 太多，但 TS / TDD / TP 的平均獲利不差
- 目前交易幾乎都是 Absorb，Initiative 幾乎沒有出現
- entry_delay_bars 目前幾乎全部為 1

請你重新從資料驗證這些結論，不要只相信描述。

請回答：

1. v6.1 虧損主要來自：
   - long 還是 short？
   - 哪個 regime？
   - 哪個 session hour？
   - 哪個 exit label？
   - 哪個 wick_type？

2. v6.1 是否存在 gross edge？
   - 請比較扣費前與扣費後。
   - 如果扣費前有 edge、扣費後沒有，請說明是 turnover / fee / trade quality 哪個問題。

3. Long side 是否值得保留？
   - long-only 回測結果如何？
   - long + neutral / range 是否比全策略穩？

4. Short side 是否應該關閉？
   - short-only 結果如何？
   - short 在哪些 regime 還有可能保留？
   - short neutral 是否應該直接禁止？

5. TDD 是否有價值？
   - TDD 次數多少？
   - TDD 的 avg pnl / avg R / avg MFE / avg MAE 如何？
   - TDD 是保護收益還是過早出場？

6. 目前 RR=2.5 是否合理？
   - 請用 MFE_R 分布回答。
   - 有多少比例的交易到過：
     0.5R
     1.0R
     1.5R
     2.0R
     2.5R
   - 如果多數交易到不了 2.5R，請測試 partial TP。

============================================================
四、請設計並執行優化實驗
============================================================

請不要只跑單一最佳參數。
請做 controlled experiments。

------------------------------------------------------------
Experiment A：Baseline
------------------------------------------------------------

A0: v6.1 原始版本

輸出：
- total return
- trades
- win rate
- PF
- net pnl
- fees
- max drawdown
- long summary
- short summary
- regime summary
- exit summary

------------------------------------------------------------
Experiment B：Side filter
------------------------------------------------------------

B1: long-only
- enable_long=True
- enable_short=False

B2: short-only
- enable_long=False
- enable_short=True

B3: long-only + disable trend_down
- long allowed only in neutral / range / trend_up

B4: short-only + disable neutral
- short 禁止 neutral
- 其他 regime 保留

請比較每個版本是否改善：
- PF
- max drawdown
- return
- trade count
- fee drag
- expectancy

------------------------------------------------------------
Experiment C：Regime filter
------------------------------------------------------------

請測試以下版本：

C1:
- long allowed: neutral, range
- short disabled

C2:
- long allowed: neutral, range, trend_up
- short disabled

C3:
- long allowed: neutral, range
- short allowed: trend_up only

C4:
- long allowed: neutral, range
- short allowed: range, trend_up

C5:
- long disabled in trend_down
- short disabled in neutral

請輸出每組結果，不要只挑最好的。

------------------------------------------------------------
Experiment D：Entry quality
------------------------------------------------------------

請測試：

D1: zoom_entry_delta_eff_threshold = 0.35
D2: zoom_entry_delta_eff_threshold = 0.40
D3: zoom_entry_delta_eff_threshold = 0.45

和 baseline 0.30 比較。

觀察：
- 交易數是否下降
- 勝率是否提升
- PF 是否提升
- fee 是否下降
- long / short 是否不同

如果可以，請 long / short 分開測：
- long_zoom_entry_delta_eff_threshold
- short_zoom_entry_delta_eff_threshold

------------------------------------------------------------
Experiment E：Stop / entry volatility normalization
------------------------------------------------------------

目前：
- entry_atr_cap = 0.35
- stop_atr_mult = 0.25

請測試小範圍 grid，不要過度最佳化：

entry_atr_cap:
- 0.25
- 0.35
- 0.45

stop_atr_mult:
- 0.15
- 0.25
- 0.35
- 0.50

請輸出 grid result。

注意：
- 不要只看 return。
- 同時看 PF、win rate、max DD、avg R、trade count。
- 如果某組只是因為交易數太少而變好，要特別註記。

------------------------------------------------------------
Experiment F：RR / partial TP
------------------------------------------------------------

目前 rr = 2.5。

請測試：

F1: rr = 1.5
F2: rr = 2.0
F3: rr = 2.5 baseline
F4: rr = 3.0

另外測 partial TP：

F5:
- 1.5R 出 50%
- 剩下 50% 使用 trailing_stop_mode="lock_tp" 或 delta trailing

F6:
- 1.0R 出 50%
- 剩下 50% 使用 trailing

請比較：
- 勝率
- 平均獲利
- 平均虧損
- PF
- 最大回撤
- equity curve smoothness
- 是否降低 SL 壓力

------------------------------------------------------------
Experiment G：Trailing mode
------------------------------------------------------------

請測：

G1: trailing_stop_mode = "lock_tp"
G2: trailing_stop_mode = "breakeven_cost"

請比較：
- TS 次數
- TDD 次數
- TP 次數
- avg win
- right-tail trades
- max drawdown
- 是否有把 2.5R 獲利吐回接近 0 的問題

------------------------------------------------------------
Experiment H：zoom_bars
------------------------------------------------------------

目前 zoom_bars = 1。

請測：

H1: zoom_bars = 1
H2: zoom_bars = 2
H3: zoom_bars = 3

請輸出 entry_delay_bars summary：
- delay=1
- delay=2
- delay=3

觀察：
- 第 2、3 根 reclaim 是否仍有 edge
- 還是只會增加低品質交易

------------------------------------------------------------
Experiment I：Session hour filter
------------------------------------------------------------

請先不要過擬合，但可以做粗略測試：

I1: 只允許 UTC 7~10
I2: 禁止 UTC 6, 9, 11, 16
I3: 只允許原本表現 PF > 1.2 的 hour，但請標記這是高過擬合風險

請比較跨年份結果，如果只有一年有效，不要當正式結論。

============================================================
五、請做跨年份驗證
============================================================

如果資料可用，請至少切成以下區間：

1. 2020-04-14 ~ 2021-04-13
2. 2021-04-14 ~ 2022-04-13
3. 2022-04-14 ~ 2023-04-13
4. 2023-04-14 ~ 2024-04-13
5. 2024-04-14 ~ 2025-04-13
6. 2025-04-14 ~ 2026-04-13

如果沒有全部資料，請用可用資料。

每個候選版本都要看：
- 各年份 PF 是否穩定
- 是否只有某一年特別好
- max drawdown 是否可接受
- trade count 是否足夠
- long/short 是否表現一致
- regime 依賴是否合理

請不要選擇只在單一年份最好的版本。

我更重視：
- 跨年份 PF 穩定 > 單一年報酬最高
- drawdown 可控 > 報酬最高
- trade count 足夠 > 少數幸運交易
- gross edge 扣掉成本後仍存在

============================================================
六、輸出格式
============================================================

請輸出以下內容：

1. Code 修正摘要
   - 修了哪些 bug
   - 改了哪些策略行為
   - 有哪些相容性處理

2. Baseline v6.1 診斷報告
   - 策略虧損來源
   - long / short 分析
   - regime 分析
   - exit label 分析
   - fee drag 分析
   - MFE / MAE 分析

3. Experiment Results 表格
   每個實驗至少包含：
   - version name
   - trades
   - win rate
   - PF
   - net pnl
   - return
   - max drawdown
   - total fees
   - avg R
   - notes

4. Top 3 候選版本
   但請不要只根據報酬排序。
   請用以下權重：
   - 跨年份穩定性
   - PF
   - max DD
   - trade count
   - fee drag
   - 邏輯合理性

5. 明確回答：
   - v6.1 是否應該保留 short？
   - 是否應該 long-only？
   - 是否應該 partial TP？
   - RR=2.5 是否過高？
   - TDD 是否保留？
   - zoom_bars 是否要放寬？
   - session filter 是否值得加入？
   - 下一版是否應該叫 v6.2？如果是，v6.2 的最小修改是什麼？

============================================================
七、限制條件
============================================================

請遵守：

1. 不要引入 lookahead bias。
2. 不要使用未來資料判斷 regime。
3. 不要用同一年度最佳化後又拿同一年度宣稱有效。
4. 不要過度 grid search。
5. 不要只追求最高報酬。
6. 每個策略變體都要保留可解釋性。
7. 若交易數太少，請標註樣本不足。
8. 若結果只是靠少數 outlier trade 撐起，請標註。
9. 不要破壞原本 v6 / v6.1 class，請新增 variant 或保留開關。
10. 所有修改請盡量保持原本 code style。