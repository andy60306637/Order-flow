# Default worker paper config. Safe to edit and keep locally.
# Paper mode uses real market data + local matching; no API key required.

mode = paper
account_alias = paper-local
exchange = binance_usdm_futures
